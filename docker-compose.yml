services:
  postgres:
    image: postgres:18.3
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ingot
      POSTGRES_USER: ingot_app
      POSTGRES_PASSWORD: ingot_dev
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
